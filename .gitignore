# LaTeX build intermediates
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.run.xml
*.toc
*.synctex.gz
*~

# Compiled CV output (built from .tex via latexmk / CI; not committed to main)
people/*/*.pdf

# Personal reference material (certificate scans, exported copies) - local only
people/*/certificates/

# macOS
.DS_Store

tmp.md
